export interface IProduct {
    active: boolean;
    description: string;
    images: string[];
    id: string;
    default_price: string;
    metadata: any; // FIXA DETTA SENARE
    name: string;
  }